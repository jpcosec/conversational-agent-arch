@startuml
start
:normalizar query (NFKD, lower);
:extraer tags por ALIASES;
if (¿hay tags??) then (yes)
else (no)
if (¿es followup? (marcador / <=4 palabras)?) then (yes)
else (no)
:forzar system:apos;
:expandir tags (EXPANSIONS);
:_rank_atoms (exact 100 / expanded 30 / lexical 8 / retained 18);
:top-5 + diff (retained/removed/added);
:construir mesa + prompt_mesa (retorna {mesa, prompt_mesa});
endif
endif
stop
@enduml