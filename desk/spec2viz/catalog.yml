template: __builtin_default__.html
title: KB Agent Runtime · Catálogo de arquitectura
brand_name: KB Agent Runtime
project_name: kb-agent-runtime
vistas:
  # ── Global ──────────────────────────────────────────
  - id: deployment-backend-frontend
    category: Global
    nav: Despliegue backend/frontend
    title: Despliegue · Backend vs Frontend
    desc: Separación explícita backend/frontend del runtime.
    type: deployment
    src: build/deployment.backend-frontend.mmd
    specs: [deployment.backend-frontend.yml]
    tags: [layer:global, view:deployment]

  # ── Backend · Semántico ─────────────────────────────
  - id: component-backend
    category: Backend
    nav: Componentes backend
    title: Backend · Componentes
    desc: FastAPI + MesaCompiler + agentes ADK + wrapper sldb.
    type: component
    src: build/component.backend.mmd
    specs: [backend/component.backend.yml]
    tags: [layer:backend, view:component]
  - id: sequence-chat-turn
    category: Backend
    nav: Secuencia /api/chat
    title: Backend · Secuencia · Turno /api/chat
    desc: Compilación de mesa + prompt + Gemini.
    type: sequence
    src: build/sequence.chat-turn.mmd
    specs: [backend/sequence.chat-turn.yml]
    tags: [layer:backend, view:sequence]
  - id: sequence-agent-retrieval
    category: Backend
    nav: Secuencia retrieval ADK
    title: Backend · Secuencia · Retrieval ADK
    desc: conversador → bibliotecario → kb_tools → sldb CLI.
    type: sequence
    src: build/sequence.agent-retrieval.mmd
    specs: [backend/sequence.agent-retrieval.yml]
    tags: [layer:backend, view:sequence]
  - id: state-chat-session
    category: Backend
    nav: Estado sesión
    title: Backend · Estado · Sesión de chat
    desc: Ciclo de vida de la sesión en memoria + followup.
    type: state
    src: build/state.chat-session.mmd
    specs: [backend/state.chat-session.yml]
    tags: [layer:backend, view:state]
  - id: activity-mesa-compile
    category: Backend
    nav: Actividad MesaCompiler
    title: Backend · Actividad · MesaCompiler.compile()
    desc: Ranking determinista de átomos y selección top-5.
    type: activity
    src: build/activity.mesa-compile.mmd
    specs: [backend/activity.mesa-compile.yml]
    tags: [layer:backend, view:activity]

  # ── Backend · AST real (código) ─────────────────────
  - id: ast-kb-agent
    category: Backend · AST real (código)
    nav: AST kb_agent
    title: AST real · kb_agent
    desc: Grafo módulos/clases/imports extraído del código.
    type: component
    src: build/ast.kb_agent.mmd
    specs: [backend/ast.kb_agent.yml]
    tags: [layer:backend, source:ast]
  - id: ast-kb-chat-ui
    category: Backend · AST real (código)
    nav: AST kb_chat_ui
    title: AST real · kb_chat_ui
    desc: Grafo módulos/clases/imports extraído del código.
    type: component
    src: build/ast.kb_chat_ui.mmd
    specs: [backend/ast.kb_chat_ui.yml]
    tags: [layer:backend, source:ast]

  # ── Frontend ────────────────────────────────────────
  - id: component-frontend
    category: Frontend
    nav: Componentes frontend
    title: Frontend · Componentes
    desc: UIs HTML/JS estáticas (chat + visor de átomos).
    type: component
    src: build/component.frontend.mmd
    specs: [frontend/component.frontend.yml]
    tags: [layer:frontend, view:component]
  - id: state-chat-ui
    category: Frontend
    nav: Estado UI chat
    title: Frontend · Estado · UI del chat
    desc: idle → enviando → turno → inspector → modal.
    type: state
    src: build/state.chat-ui.mmd
    specs: [frontend/state.chat-ui.yml]
    tags: [layer:frontend, view:state]
  - id: activity-atom-viewer
    category: Frontend
    nav: Actividad visor átomos
    title: Frontend · Actividad · Visor de átomos
    desc: carga atoms.json, filtra y abre átomo como markdown.
    type: activity
    src: build/activity.atom-viewer.mmd
    specs: [frontend/activity.atom-viewer.yml]
    tags: [layer:frontend, view:activity]
