# Rama de dominio — Campañas y medicamentos

Rama de `DomainAtom`s por medicamento × especialidad (`domain:medicamentos.*` × `user:specialty.*`).

**⚠ BLOQUEO REAL (2026-09-04):** el portafolio de medicamentos NO está documentado en
ninguna fuente disponible. El contenido MLR aprobado está en estado `requested`
(client-input-hcp-contenido-mlr.md) y llega vía Kiteworks/SFTP. Los átomos `ejemplo-*`
de esta carpeta son plantillas de forma para validar el pipeline y DEBEN reemplazarse
1:1 por el contenido MLR real antes del sandbox de octubre.
